{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "dotenv": "^10.0.0",
    "react": "^17.0.1",
    "react-dadata-box": "^1.3.6",
    "react-date-picker": "^8.3.5",
    "react-dom": "^17.0.1",
    "react-dotenv": "^0.1.3",
    "react-scripts": "4.0.3",
    "redux-devtools-extension": "^2.13.9",
    "styled-components": "^5.3.3",
    "web-vitals": "^1.0.1"
  },
  "devDependencies": {

  },
  "engines": {
    "node": "16.11.1",
    "npm": "6.14.9"
  },
  "scripts": {
    "start-client": "react-dotenv && react-scripts start --force",
    "build": "react-dotenv && react-scripts build && (cd ../backend && yarn install) --force",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start": "cd ../backend && yarn start"
  },
  "proxy":"http://localhost:5000/",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "react-dotenv" : {
    "whitelist" : [ "API_KEY" ]
  }
}
